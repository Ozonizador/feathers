import { createContext } from "react";

// Contexts for manipulating the user data. Can be stalled!
// export const UserContext = createContext<User | undefined>(undefined);
// export const SetUserContext = createContext<Dispatch<SetStateAction<User | undefined>>>(() => {});
